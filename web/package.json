{
  "name": "gochess-web",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=dist/bundle.js --sourcemap",
    "watch": "esbuild src/main.ts --bundle --outfile=dist/bundle.js --sourcemap --watch=forever"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
